cum funtioneaza practice ul?
  1. thema selectata pen click din unul din cele 3 btn('dragon; griffin; wizard') se va retine la refresh pag (aka localStoarge)
  2. display name -ul (textul introdus in input text) nu se va retine la refresh page (aka sessionStorage)
  
  
  3.bonus: Then write some code to clear only the currently-selected button.
